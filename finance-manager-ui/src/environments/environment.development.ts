// Overrides para DESARROLLO LOCAL.
// `ng serve` / `npm start` cargan este archivo automáticamente vía el
// `fileReplacements` de la configuración `development` en angular.json.
// El build de producción NUNCA usa este archivo.
export const environment = {
    production: false,
    apiUrl: 'http://localhost:5246/api/'
};
